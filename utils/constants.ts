/** Common name of the dummy root CA (helm-charts `root-ca.cert.pem`). */
export const ROOT_CA_COMMON_NAME = 'Dummy Root CA';

/** Certificate Type — inventory `data-testid="badge"` and API `certificateType`. */
export const CERTIFICATE_TYPE_X509_PATTERN = /X\.509/i;